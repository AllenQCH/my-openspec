# 测试规范

- 每条验收标准和每个变更后的 code_graph 分支至少映射一个测试。
- 非平凡逻辑必须有聚焦单元测试；数据库写入、Job、Consumer 和跨服务调用还必须有真实入口与可观察业务结果。
- 回归范围必须覆盖原请求结构、状态条件、存量调用方和依赖包兼容性。
- 测试证据记录实际命令、退出码、用例数量、观察结果和未执行原因；未运行不能记为通过。
- 测试环境验证必须先确认部署版本，再执行 curl、数据查询和结果校验。
