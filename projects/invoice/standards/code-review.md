# Code Review 规范

- 核对实现是否严格匹配已确认需求、设计、服务 Binding 和 Changed Files。
- 检查完整调用链中的状态条件、事务边界、并发锁、批量部分成功、空值和异常处理。
- 检查公共 DTO、Feign、序列化和接口路径对既有消费者的兼容性。
- 检查数据权限是否基于服务端真实数据，不信任调用方可伪造字段。
- 检查测试是否覆盖新增行为、边界、兼容链路和明确回归项。
- 阻塞性发现必须返回后端设计阶段，不能由 Reviewer 静默修复或豁免。
